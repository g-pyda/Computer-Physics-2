#include <stdio.h>
#include <vector>
// 126 from lecture

void process_node(
    std::vector<std::vector<double>> x,
    std::vector<std::vector<double>> y,
    int node_id,
    int N,
    double L,
    bool biparabolic
);